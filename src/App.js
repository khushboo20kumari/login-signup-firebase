import {getDatabase,ref,set} from "firebase/database"
import {app} from "./firebase";
const db=getDatabase(app)
function App(){
  return(
    <div className="App">

    </div>
  )
}
export default App;